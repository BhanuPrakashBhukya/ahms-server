spring.application.name=ahms

#spring.security.user.name=user
#spring.security.user.password=password

# PostgreSQL database connection configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres?currentSchema=hotel_management
spring.datasource.username=postgres
spring.datasource.password=postgres

# Specify HikariCP as the connection pool implementation (optional, as it's the default)
spring.datasource.type=com.zaxxer.hikari.HikariDataSource

# HikariCP specific settings
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.pool-name=MyHikariCP

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#aws email communications
spring.mail.host=email-smtp.ap-south-1.amazonaws.com
spring.mail.port=587
spring.mail.username=AKIASVLKCR5LYK2RN27H
spring.mail.password=BEWVfxIJCngf5GZqYYLftQJF8tkYRWnebGbEOFpsgGuj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

#Actuator configurations
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoint.prometheus.enabled=true
management.prometheus.metrics.export.enabled=true

# Directory for logs
logging.file.path=/logs  # Specify your directory path here
# Log filename (optional)
logging.file.name=ahms.log